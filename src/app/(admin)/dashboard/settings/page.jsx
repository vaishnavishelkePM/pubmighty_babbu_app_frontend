// src/app/dashboard/settings/page.jsx
// (Adjust the path based on your project routing structure)

import SettingsView from 'src/sections/dashboard/settings/settings-view';

export const metadata = {
  title: 'Settings | Admin',
};

export default function SettingsViewPage() {
  return <SettingsView />;
}
