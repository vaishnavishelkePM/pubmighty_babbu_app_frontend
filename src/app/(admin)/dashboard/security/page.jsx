import SettingsView from 'src/sections/dashboard/security/security-view';

export const metadata = {
  title: 'Settings | Admin',
};

export default function SettingsViewPage() {
  return <SettingsView />;
}
